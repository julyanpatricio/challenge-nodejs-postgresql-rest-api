const { Router } = require('express');
const router = Router();
const {} = require('../db')


router.get('/', async (req, res, next) => {
   try {
      
      
   } catch (error) {
      next(error)
   }
})


module.exports = router;
